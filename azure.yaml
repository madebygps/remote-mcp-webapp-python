# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: remote-mcp-webapp-python
metadata:
  template: remote-mcp-webapp-python@0.0.1-beta
infra:
  provider: bicep
services:
  web:
    project: src
    language: python
    host: appservice